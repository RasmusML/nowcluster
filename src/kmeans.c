#include <stdio.h>
#include <stdlib.h>

// #include "kmeans.h"
#include "types.h"
#include "logger.h"

void kmeans(int32 dataset, uint32 num_clusters) {
  kmeans_log();
  printf("clusters %d\n", num_clusters);
}

